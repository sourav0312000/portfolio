import 'package:flutter/material.dart';
class hobbies extends StatelessWidget {
  const hobbies({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Co-curriculars"),
        backgroundColor: Colors.green[200],
      ),

    );
  }
}
